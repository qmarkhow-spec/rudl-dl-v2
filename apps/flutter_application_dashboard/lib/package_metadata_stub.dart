import 'package_metadata_types.dart';

class ApkMetadataReader {
  static Future<PackageMetadata?> read(String path) async => null;
}

Future<PackageMetadata?> readIpaMetadata(String path) async => null;
